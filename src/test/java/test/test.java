package test;

import com.albares.db.Db;
import com.albares.domain.User;
import java.sql.SQLException;

public class test {

    public static void main(String[] args) {
        /*
        try {
            Db myDb = new Db();
            User u = new User();
            String name = "Juan";
            //u.setId(1);
            myDb.connect();
            System.out.println("score: " + u.selectScore(myDb));

            myDb.disconnect();
        } catch (SQLException e) {

        }
        */
    }
}
